language: python

matrix:
  include:
    - python: 3.7
      dist: xenial
      sudo: true

install:
  - pip install coverage
  - pip install -U pytest
  - pip install robotframework

script: 

  - cd tests/
  - cd test_files
  - pytest ./
